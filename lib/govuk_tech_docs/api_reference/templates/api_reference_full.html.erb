## API info

API version: v<%= @document.info.version %>
<% if @document.info.description %>
Description: <%= @document.info.description %>
<% end %>

<% unless @document.servers.empty? %>
Servers:

<% @document.servers.each do |server| %>
- <%= server.description %>: <%= server.url %>
<% end %>
<% end %>

<% @document.paths.each do |path_name, path| %>
<%= render_operations_block(path_name, path) %>
<% end %>

## Schemas

<% @document.components.schemas.each do |schema_name, schema| %>
<%= render_schema_block(schema_name, schema) %>
<% end %>
