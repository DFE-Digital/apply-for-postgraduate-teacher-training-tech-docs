---
title: Resources and their attributes
weight: 70
---

# Resources and their attributes

## Strings

For attributes that are of **string** type, they will have a maximum length of 255 characters unless specified otherwise.

## Application

```json
<%= single_application_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: application_attributes } %>

## Candidate

```json
<%= candidate_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: candidate_attributes } %>

## Contact details

```json
<%= contact_details_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: contact_details_attributes } %>

## Course

```json
<%= course_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: course_attributes } %>

## Qualification

```json
<%= qualification_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: qualification_attributes } %>

## Work experience

```json
<%= work_experience_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: work_experience_attributes } %>

## Reference

```json
<%= reference_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: reference_attributes } %>

## Offer

```json
<%= offer_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: offer_attributes } %>

## Withdrawal

The withdrawal of an application can only be made by a candidate.

```json
<%= withdrawal_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: withdrawal_attributes } %>

## Rejection

```json
<%= rejection_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: rejection_attributes } %>

## HESA ITT Data

Information required by HESA for the Initial Teacher Training data
return. This information will only be returned once the application
status is `enrolled`.

If the candidate declined to provide a piece of information, its value
will be the approriate HESA code for "information refused", or if this is
not supported by HESA, `null`. Therefore fields not supporting an
"information refused" code are marked optional.

```json
<%= hesa_itt_data_json %>
```

<%= partial 'partials/attribute_table', locals: { attributes: hesa_itt_data_attributes } %>
